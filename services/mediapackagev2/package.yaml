# Generated by stratosphere-gen
_common/package: !include "../../common/package.yaml"

name: stratosphere-mediapackagev2
version: 1.0.1
synopsis: Stratosphere integration for AWS MediaPackageV2.
description: Integration into stratosphere to generate resources and properties for AWS MediaPackageV2
category: AWS, Cloud, MediaPackageV2
license-file: LICENSE.md
library:
  source-dirs: gen
  ghc-options:
  - -Wno-unused-imports
<<: *defaults

dependencies:
- aeson >= 2 && < 3
- base  >= 4.8  && < 4.22
- stratosphere == 1.0.1
