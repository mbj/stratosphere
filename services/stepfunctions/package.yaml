# Generated by stratosphere-gen
_common/package: !include "../../common/package.yaml"

name: stratosphere-stepfunctions
version: 1.0.1
synopsis: Stratosphere integration for AWS StepFunctions.
description: Integration into stratosphere to generate resources and properties for AWS StepFunctions
category: AWS, Cloud, StepFunctions
license-file: LICENSE.md
library:
  source-dirs: gen
  ghc-options:
  - -Wno-unused-imports
<<: *defaults

dependencies:
- aeson >= 2 && < 3
- base  >= 4.8  && < 4.22
- stratosphere == 1.0.1
