# Generated by stratosphere-gen
_common/package: !include "../../common/package.yaml"

name: stratosphere-iotwireless
version: 1.0.0
synopsis: Stratosphere integration for AWS IoTWireless.
description: Integration into stratosphere to generate resources and properties for AWS IoTWireless
category: AWS, Cloud, IoTWireless
license-file: LICENSE.md
library:
  source-dirs: gen
  ghc-options:
  - -Wno-unused-imports
<<: *defaults

dependencies:
- aeson >= 2 && < 3
- base  >= 4.8  && < 4.22
- stratosphere == 1.0.0
